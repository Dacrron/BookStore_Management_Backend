@model IEnumerable<Bookietry.Models.Inventory>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table table-dark table-borderless table-hover">
    <thead>
        <tr>
            <th>
                @*@Html.DisplayNameFor(model => model.forkey4)*@
                Book Name
            </th>
            <th>
                @Html.DisplayNameFor(model => model.quantity)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.forkey4.Book_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.quantity)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Book_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Book_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Book_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
